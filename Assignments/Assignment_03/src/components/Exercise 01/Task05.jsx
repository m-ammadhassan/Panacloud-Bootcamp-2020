import React from 'react'

const Task05 = () => {
  return (
    // Want to style it with CSS? Instead of using the “class” property like you would in HTML, use “className”. 
    // Then create a file src/index.css with the styles, and add the line import './index.css' to the top of index.js. 
    // Yep, you can import CSS into JS. Sorta. That’s Webpack working its magic behind the scenes.
    <div className='aim'>
        <h4>Aim:</h4>
        <p>
            To learn high-level skills to build Full Stack Serverless Multi-Tenant SaaS Apps and get ready myself for the in
        </p>
    </div>
  )
}

export default Task05